(() => { const form=document.querySelector('#labelScannerForm'), result=document.querySelector('#scannerResult'); if(!form)return; form.addEventListener('submit',async e=>{e.preventDefault();result.textContent='Validando…';const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});const data=await response.json();result.innerHTML=data.traceCode?`<strong>${data.status}</strong><span>${data.traceCode} · ${data.type} · ${data.template}</span><a href="${data.url}">Abrir rastreio</a>`:`<strong>${data.status}</strong><span>${data.message}</span>`;form.querySelector('#code').select();}); })();
