(() => {
 const page=document.querySelector('[data-parameters-page]'); if(!page)return;
 const details=document.getElementById('parameterDetailsDialog'), body=document.getElementById('parameterDetailsBody'), title=document.getElementById('detailName'), copy=document.getElementById('copyParameterKey'), edit=document.getElementById('editParameterLink'); let currentKey='';
 const esc=v=>String(v??'—').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
 document.querySelectorAll('.js-parameter-details').forEach(b=>b.addEventListener('click',async()=>{details.showModal();title.textContent='Carregando…';body.innerHTML='<div class="parameters-empty"><span class="spinner-border"></span><span>Consultando dados seguros…</span></div>';try{const r=await fetch(`/Parameters/Details/${b.dataset.id}`,{headers:{Accept:'application/json'}});if(!r.ok)throw new Error();const x=await r.json();currentKey=x.code;title.textContent=x.name;edit.href=`/Parameters/Edit/${x.id}`;body.innerHTML=`<div class="parameter-detail-grid"><div><small>Chave</small><strong>${esc(x.code)}</strong></div><div><small>Grupo</small><strong>${esc(x.categoryName)}</strong></div><div><small>Status</small><strong>${x.isActive?'Ativo':'Inativo'}</strong></div><div><small>Tipo</small><strong>Cadastro operacional</strong></div><div><small>Valor / referência</small><strong>${esc(x.abbreviation||x.externalCode||x.parentName)}</strong></div><div><small>Última alteração</small><strong>${new Date(x.updatedAt||x.createdAt).toLocaleString('pt-BR')}</strong></div></div><p class="mt-3 mb-0">${esc(x.description||'Sem descrição informada.')}</p>`}catch{title.textContent='Não foi possível carregar';body.innerHTML='<p>Confira sua conexão e tente novamente. Nenhuma alteração foi realizada.</p>'}}));
 document.querySelectorAll('.js-dialog-close').forEach(b=>b.addEventListener('click',()=>b.closest('dialog').close()));document.querySelectorAll('dialog').forEach(d=>d.addEventListener('click',e=>{if(e.target===d)d.close()}));
 copy.addEventListener('click',async()=>{if(!currentKey)return;await navigator.clipboard.writeText(currentKey);copy.textContent='Chave copiada';setTimeout(()=>copy.textContent='Copiar chave',1800)});
 const del=document.getElementById('parameterDeleteDialog'),form=document.getElementById('parameterDeleteForm');document.querySelectorAll('.js-parameter-delete').forEach(b=>b.addEventListener('click',()=>{form.action=`/Parameters/Delete/${b.dataset.id}`;document.getElementById('deleteParameterName').textContent=b.dataset.name;del.showModal()}));
})();
