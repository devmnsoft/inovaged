(()=>{const video=document.querySelector('[data-qr-video]'),input=document.querySelector('[data-qr-input]'),start=document.querySelector('[data-qr-start]');if(!start)return; if(!('BarcodeDetector'in window)){start.disabled=true;start.textContent='Câmera indisponível — use o campo manual';return}let stream,active=false;start.addEventListener('click',async()=>{try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});video.srcObject=stream;await video.play();active=true;start.hidden=true;const detector=new BarcodeDetector({formats:['qr_code']});const tick=async()=>{if(!active)return;for(const code of await detector.detect(video)){input.value=code.rawValue;active=false;stream.getTracks().forEach(x=>x.stop());input.form?.requestSubmit();return}requestAnimationFrame(tick)};tick()}catch{start.textContent='Não foi possível abrir a câmera';start.disabled=true}})})();
