(()=>{const search=document.querySelector('[data-binding-search]'),type=document.querySelector('[data-binding-type]'),status=document.querySelector('[data-binding-status]'),rows=[...document.querySelectorAll('[data-binding-row]')];const filter=()=>rows.forEach(row=>{const matchesText=!search.value.trim()||row.dataset.search.includes(search.value.trim().toLowerCase());const matchesType=!type.value||row.dataset.type.includes(type.value);const matchesStatus=!status.value||row.dataset.status===status.value;row.hidden=!(matchesText&&matchesType&&matchesStatus);});[search,type,status].forEach(control=>control?.addEventListener('input',filter));})();
