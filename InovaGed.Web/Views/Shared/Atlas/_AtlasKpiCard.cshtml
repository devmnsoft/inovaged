@{var label=ViewData["Label"]?.ToString()??"Indicador";var value=ViewData["Value"]?.ToString()??"—";var hint=ViewData["Hint"]?.ToString();}<article class="atlas-kpi-card"><small>@label</small><strong>@value</strong>@if(hint is not null){<span class="text-muted">@hint</span>}</article>
