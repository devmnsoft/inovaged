# Diagnóstico Evolução 04.1.5

- SHA inicial registrado: `4be8eec95af39ab83f372e57a6438d60d34be6cf`.
- Branch de trabalho: `codex/homologar-cms-corrigir-protocolo-ci`.
- Workflows existentes: `.github/workflows/ci.yml` (`server-linux`, `agent-windows`, `security-guards`) e `.github/workflows/dotnet-ci.yml` (`specialized-guards`).
- Causa provável da ausência do CI principal nos PRs 280-283: não há metadados remotos do GitHub disponíveis neste container; no código, `ci.yml` já existia, mas o job `Agent doctor` mascarava falha com `|| echo`, permitindo diagnóstico falso. Se o arquivo não aparecer no GitHub, o administrador deve verificar Actions habilitado no repositório, branch base contendo `.github/workflows/ci.yml`, permissões de Actions e filtros de branch.
- Correções efetuadas: `actionlint` passou a ser gate inicial; `Agent doctor` não mascara falhas; `dotnet-ci.yml` ganhou `workflow_dispatch`; migrations finais usam `\ir` relativo ao script; capability de conteúdo passa a ser resolvida e consumida sem claims/cookie do navegador; URL de conteúdo passa a usar `DigitalSignature:PublicServerBaseUrl`; conclusão recebe comando tipado no controller e valida usuário no orquestrador; hash de idempotência passa a incluir sessão, tenant, usuário, CMS, certificado, cadeia, operação e versão do agente; agente ganhou CLI real `doctor/version/install/uninstall/rotate-certificate/serve`; operações do agente usam autenticação por headers; downloader protegido é usado pelo `OperationStore`.
- Resultado dos gates locais: `dotnet` não está instalado no ambiente (`/bin/bash: dotnet: command not found`), então restore/build/test/migrations não puderam ser executados localmente. Os jobs obrigatórios devem executar no GitHub Actions antes de marcar a PR como ready.
