# Manifesto de backup

`BackupManifest` e `BackupManifestArtifact` são contratos tipados. O formato registra identificadores, horários UTC, versões, consistência, estado, correlação e artefatos com caminho relativo, tamanho e SHA-256. Connection string, senha, usuário, token, chave e caminhos absolutos não pertencem ao contrato.
