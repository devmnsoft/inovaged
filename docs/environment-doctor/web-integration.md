# Integração Web

A Central de Prontidão deve ler o último resultado seguro persistido. Uma solicitação autorizada apenas enfileira a execução; processos externos nunca devem ser executados durante a renderização nem bloquear a requisição HTTP. A UI e persistência não foram alteradas neste hotfix.
