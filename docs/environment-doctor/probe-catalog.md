# Catálogo de probes

`IEnvironmentProbe` define código, categoria, ordem e execução assíncrona. A implementação do catálogo completo e sua composição por DI permanecem trabalho posterior; nenhum mecanismo de reflection aberta foi introduzido.
