# Alias de System.Environment

Código no namespace `InovaGed.Environment.*` deve declarar `using BclEnvironment = global::System.Environment;` e usar `BclEnvironment.*`. O teste `SystemEnvironmentNamespaceCollisionTests` impede referências simples às APIs críticas.
