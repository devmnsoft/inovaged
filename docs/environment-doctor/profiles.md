# Perfis

Os perfis planejados são `quick`, `full` e `deployment`. Nesta entrega, a compatibilidade dos comandos clássicos `check`, `check --json` e `report` foi preservada; o parser de perfis ainda não foi conectado à CLI.
