# Catálogo de checks

Categorias: TOOLCHAIN (SDK, global.json, TFM, MSBuild), DATABASE/MIGRATIONS (readiness compartilhada), STORAGE (configuração e probe), PREVIEW (LibreOffice), OCR (estado/executável), IIS (aplicabilidade), SIGNING e WORKERS (orientação segura). Status: PASS, WARNING, FAIL, NOT_APPLICABLE e NOT_VERIFIABLE.
