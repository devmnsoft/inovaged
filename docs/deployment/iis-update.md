# Atualização IIS

Extraia em uma nova pasta de release, homologue em binding `127.0.0.1` e app pool temporário, execute Doctor/smoke tests, altere o physical path, recicle e valide readiness. Nunca sobrescreva `current`.
