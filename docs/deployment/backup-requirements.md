# Backup

Quando `requireDatabaseBackup=true`, evidência recente, íntegra, restaurável e do escopo correto é bloqueante. Mascarar localização e nunca imprimir credenciais. Preferir o módulo de continuidade.
