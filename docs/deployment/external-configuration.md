# Configuração externa

Defina `INOVAGED_CONFIG_PATH` para um JSON absoluto fora da release. Em produção o arquivo é obrigatório quando utilizado e não recarrega automaticamente. O pacote leva somente placeholders.
