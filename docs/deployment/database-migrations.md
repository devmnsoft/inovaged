# Migrations

Use status/compatibility/apply/verify do Database Migrator e o manifesto. Automação aceita apenas Additive. RequiresMaintenance exige aprovação; DestructiveBlocked e ManualOnly bloqueiam. Não há rollback automático do banco.
