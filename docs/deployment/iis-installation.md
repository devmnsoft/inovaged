# Instalação IIS

Execute primeiro `Validate`. Use `Install` somente para site isolado ou inexistente. Crie diretórios/ACLs de menor privilégio e confirme backup antes do Migrator. Nunca teste contra site existente.
