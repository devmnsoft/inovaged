# Visual Studio

Importe `.vsconfig`, feche o Visual Studio, instale o SDK 8 e confirme a pasta `C:\Program Files\dotnet\sdk`. Remova apenas `.vs`, `bin` e `obj`, reabra `InovaGed.sln`, restaure e faça Rebuild. Preserve código, appsettings locais, storage, bancos, user secrets e certificados.
