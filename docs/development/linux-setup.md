# Desenvolvimento no Linux

Instale o SDK .NET 8 pelo repositório oficial da distribuição ou use uma imagem homologada. Execute `./eng/setup-development.sh` e `./eng/build.sh`. O fluxo é independente da distribuição; PostgreSQL, LibreOffice e OCR permanecem dependências configuradas pelo operador.
