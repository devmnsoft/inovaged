# Migração Signature CMS Runtime

A migration oficial é `database/migrations/2026_07_signature_cms_agent_runtime.sql` e está integrada em `database/apply_all_required_migrations.sql`. Ela é aditiva, idempotente, reaplicável e compatível com execuções manuais anteriores da migration em `sql/`.
