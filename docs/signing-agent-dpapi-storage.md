# Signing Agent DPAPI Storage

No Windows, o agente deve persistir pairings, origem, expiração, token, certificado HTTPS local, versão do protocolo e installation id usando DPAPI do usuário atual. O agente não deve persistir PIN, chave privada exportada, documento, CMS ou conteúdo temporário após uso.
