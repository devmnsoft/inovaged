# Diagnóstico de estabilização Administração/Continuidade

- SHA inicial: `d4dd820c77b244ebf8a1e06edea2affdb54f80fc`.
- Branch de trabalho: `codex/estabilizar-admin-continuity-ci`.
- Ambiente local: `dotnet` indisponível (`dotnet: command not found`), portanto os comandos finais foram registrados como bloqueados por limitação do container.

| Erro observado | Causa | Arquivo responsável | Impacto | Correção adotada | Validação executada | Risco residual |
| --- | --- | --- | --- | --- | --- | --- |
| `InovaGed.sln` possuía blocos `Project/Global` após o fim lógico da solution. | Regeneração anterior deixou duplicidade pós-`EndGlobal`. | `InovaGed.sln` | Restore/list/build da solution falhavam. | Removido bloco duplicado e preservados oito projetos únicos em Debug/Release. | Inspeção estrutural e teste `SolutionAndContinuityArchitectureTests`. | Revalidar com `dotnet sln list` em ambiente com SDK. |
| Restore não pôde ser executado no container. | SDK .NET ausente. | Ambiente | Impede comprovar restore/build/test localmente. | CI permanece responsável por execução real em Ubuntu com .NET 8. | `dotnet --info` retornou `command not found`. | Necessário acompanhar CI remoto. |
| Dependências Npgsql incompatíveis. | `Npgsql 10.x` misturado com provider EF Core PostgreSQL 8.x. | `*.csproj` | Risco de NU1605 e incompatibilidade com .NET 8. | Alinhado `Npgsql` para 8.0.6 e removido do Domain. | Teste arquitetural valida Domain sem Npgsql. | Confirmar árvore transitiva no CI. |
| Guard de DI frágil por formatação. | Workflows buscavam strings exatas em uma linha. | `.github/workflows/*.yml` | Falso negativo quando o código era reformatado. | Adicionado teste C# de composição/guardrails e harmonização planejada dos workflows. | Teste fonte em `InovaGed.Application.Tests`. | CI ainda deve rodar validações shell existentes. |
| `DatabasePermissionChecker` sempre negava. | Implementação placeholder. | `InovaGed.Application/Administration/PermissionEnforcement.cs` | `ENFORCED` bloquearia todos. | Consulta real tenant-scoped em `ged.app_user`, `ged.user_role`, `ged.app_role`, `ged.role_permission`, `ged.permission`. | Teste fonte valida ausência de `Task.FromResult(false)` e filtro tenant. | Validar nomes finais de colunas em homologação. |
| Roles do login ignoravam tenant. | SQL usava só `user_id`. | `InovaGed.Infrastructure/Auth/AuthRepository.cs` | Role de outro tenant poderia conceder acesso. | Adicionado `ur.tenant_id=@tenantId` e join tenant-scoped. | Teste fonte. | Validar índices em banco real. |
| Dashboard administrativo consultava tabelas fictícias. | Uso de `users`, `tenants`, `roles`, `permissions`. | `InovaGed.Infrastructure/Administration/AdministrationDashboardService.cs` | Indicadores incorretos e zeros silenciosos. | Alterado para tabelas canônicas e removido catch que convertia erro em zero. | Inspeção via `rg`. | Pode requerer adapters em ambientes legados. |
| `ProcessDueJobsAsync` retornava zero. | Worker não fazia claim persistente. | `InovaGed.Infrastructure/Continuity/ContinuityServices.cs` | Jobs nunca processavam. | Implementado claim transacional com `FOR UPDATE SKIP LOCKED`, lease e conclusão mínima. | Inspeção fonte. | Execução real de backup ainda depende de fatia operacional e pg_dump. |
| Verificador usava `StartsWith(root)` frágil. | Sem normalização com separador e sem bloqueio de extras. | `InovaGed.Portability.Verifier/Program.cs` | Risco de path traversal/arquivos alterados. | Normalização canônica, bloqueio de absolutos/reparse point, duplicados e extras. | Inspeção fonte. | Testar executável em CI com SDK. |
