# Pairing do Signing Agent

O pairing usa origem exata, código temporário, token aleatório, expiração curta e revogação. Em Windows produtivo, o armazenamento local deve ser protegido por DPAPI vinculada ao usuário. Pairing silencioso e origem `*` são proibidos.
