# Segurança do Signing Agent

O agente valida loopback na inicialização, não usa CORS wildcard, exige pairing, confirmação explícita e bloqueia URLs de conteúdo com HTTP, credenciais ou redes privadas. A chave privada, PIN, senha PFX e material exportável nunca são enviados ao servidor.
